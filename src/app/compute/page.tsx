import { ComingSoon } from "@/components/coming-soon";

export default function ComputePage() {
  return (
    <ComingSoon
      title="Tax Computation is being built next"
      description="A deterministic, rules-based calculation engine with versioned rate tables — the LLM guides inputs and drafts document extractions, but never performs the arithmetic."
    />
  );
}
