import { ComingSoon } from "@/components/coming-soon";

export default function SourcesPage() {
  return (
    <ComingSoon
      title="Source Library is being built next"
      description="Every Act section, HMRC manual paragraph, and tribunal case cited anywhere in the app will have its own canonical page here, with backlinks to everywhere it's used."
    />
  );
}
