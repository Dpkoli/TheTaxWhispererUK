import { ComingSoon } from "@/components/coming-soon";

export default function AdvisoryPage() {
  return (
    <ComingSoon
      title="Advisory is being built next"
      description="Structured, cited answers with applicable law, HMRC manual references, case law, worked examples, and a confidence flag on every claim."
    />
  );
}
